p({
  class: 'foam.nanos.notification.email.EmailServiceConfig',
  id: 'smtp'
})
p({
  class: 'foam.nanos.notification.email.EmailServiceConfig',
  documentation: 'this protocol used to send an email to emails that were recieved. So like an automated response?',
  id: 'imap',
  protocol: 'imaps',
  port: '993'
})
